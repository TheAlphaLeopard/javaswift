{
  "name": "pronto-lang",
  "version": "1.0.0",
  "description": "Pronto is a fast, simple, and powerful programming language designed for the web, compiling to JavaScript.",
  "main": "dist/compiler/index.js",
  "scripts": {
    "build": "tsc && cp -r src/examples dist/ && cp src/index.html dist/",
    "start": "npm run build && node dist/server.js",
    "test": "jest --config jest.config.js",
    "clean": "rm -rf dist",
    "watch": "tsc --watch"
  },
  "keywords": [
    "programming",
    "language",
    "web",
    "compiler",
    "typescript"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/jest": "^26.0.24",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.6",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  }
}
